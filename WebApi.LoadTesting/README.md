# WebApi.LoadTesting

Console load tester for Meeting Room Web API.

Start the API:

```powershell
dotnet run -c Release -- --requests 100000000 --concurrency 100
```

Defaults: [`http://localhost:5119`](http://localhost:5119)
